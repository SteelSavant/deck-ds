[package]
name = "schema_derive"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["std", "derive"] }
schemars = { version = "1.2.1", features = [
    "derive",
    "schemars_derive",
    "std",
    "preserve_order",
] }
dyn-clone = "1.0.20"
